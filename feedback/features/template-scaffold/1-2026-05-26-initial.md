# 1 — initial

> 日期：2026-05-26
> 涉及代码：stats/tokens.jsonl, stats/summaries/, feedback/features/
> 类型：新建

## Plan

初始模板创建时的基础设施文件：
- `stats/tokens.jsonl`：空的 JSONL 文件，用于记录每次 agent commit 的 token 消耗
- `stats/summaries/.gitkeep`：保持空目录在 git 中可追踪
- `feedback/features/.gitkeep`：保持业务特性档案目录在 git 中可追踪

这些文件本身不包含业务逻辑，仅为框架的运行时状态存储提供起点。

## 改动要点

- 无 API 选型，均为空占位文件
- `stats/tokens.jsonl` 将由 `scripts/log-tokens.sh --from-commit HEAD` 自动追加内容

## 验证结果

- check-api-parity：N/A（非代码文件）
- check-consistency：PASS（WARN 为预期的初始状态）

## 对话摘要

用户直接确认，无调整。

## 残留 / 下一轮

- [x] 完成初始模板仓库搭建
- [ ] 使用者按项目实际情况填充 check-api-parity.sh 规则
