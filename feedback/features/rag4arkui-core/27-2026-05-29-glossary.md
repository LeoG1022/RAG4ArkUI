# 27 — glossary

> 日期：2026-05-29
> 涉及代码：
> - `docs/GLOSSARY.md`（新建 · 术语对照表 · 含 agent 自造词 / 项目惯用词 / RAG 业界标准 / 协议层 / pre-existing 状态形容词 5 类）
> - `mdbook/src/reference/glossary.md`（新建 · `{{#include}}` 引 docs/GLOSSARY.md）
> - `mdbook/src/SUMMARY.md`（参考节加 「术语对照表」）
> - `docs/ROADMAP.md`（修一处 `<name>` 反引号转义 · 避免 mdBook HTML parser 告警）
> 类型：新建（用户向文档 + 防漂移声明 · 用户反馈「真活」自造词后立即修复）

## 本轮目标

用户指出我从 commit 1 起反复用「真活」这个**自造词**（实际不是标准中文表达），22 commit / 19 STATUS / 12 meta 里散落几十处。承诺：
- **A**：以后新文档不再用「真活」· 改用「实装 / 真实可用 / 端到端跑通 / 真启动」
- **C**：加术语对照表（`docs/GLOSSARY.md`）· 链入 mdBook · 帮新读者解读历史归档

不做 B（历史 sed 替换）—— 同义改写不创造新价值 · 反而污染 commit log。

## Plan

> 必备节（FAIL 级 M-FEATURE-PLAN）

### 内容范围（5 类术语）

1. **Agent 自造词**：真活 / 真启动 / 真上线 → 对应标准中文
2. **项目惯用词**：meta vs business / 双轨归档 / STATUS-PER-ROUND / M-XXX / 第 N 次实战
3. **RAG 业界标准**：Hybrid / RRF / Reranker / HyDE / Parent-Child / Citation
4. **协议层标准**：MCP / LSP / JSON-RPC 2.0 / Content-Length framing / stdio server
5. **pre-existing 状态形容词**：pre-existing 阻塞 / stub / feature gated / shadow

### 文件位置决策

`docs/GLOSSARY.md`（单一信任源）+ `mdbook/src/reference/glossary.md` 仅 1 行 `{{#include}}` ——
与 Day 22 mdBook 的策略一致（docs/ 是源 · mdbook/src 只做导航）。

加入 SUMMARY.md 「参考」节末尾。

### 防漂移机制

GLOSSARY.md 末尾写「未来防漂移」节明确声明：
- 自此 commit 起 · 新归档不用「真活」
- 新增 agent 自造词 → 先加本文件一行 → 再用
- 历史不重写

### 替代方案权衡（被否）

- 备选 B：全 repo `sed -i 's/真活/实装/g'` 历史 commit
  - 否决：22 commit + 19 STATUS + 12 meta 全 sed 后 1-2 commit 清扫量 · 同义替换不创造新价值 · 反而污染 git blame
- 备选 D：把 GLOSSARY 放 `feedback/` 下
  - 否决：feedback/ 是归档 · 不该放查阅类文档 · `docs/` 更合适

### 测试策略

1. ✅ `mdbook build` 站点正常 + GLOSSARY 出现在导航
2. ✅ 顺带修 ROADMAP `<name>` HTML parser 告警 → clean build 0 warning
3. ⏳ 自此 commit 起 · agent 自我约束不再用「真活」（行为改变 · 无自动校验）

## 改动要点

> API 选型 / 算法 / 关键决策

### 与上一轮（task #81）的差异

- crate 数 9（不变）
- Rust 代码（不动）
- 新增 2 个 .md（docs/GLOSSARY + mdbook 引用）
- 改 mdbook/src/SUMMARY.md + docs/ROADMAP.md 各 1 处

### 关键决策

1. **A + C 不做 B**：行为改 + 加对照表 · 不擅自重写历史 commit
2. **GLOSSARY 5 类分组**：让读者能按需查（不只「真活」一项 · 覆盖项目所有可能困惑词）
3. **`{{#include}}` 单一信任源**：docs/GLOSSARY.md 是源 · mdbook 引用 · 与 Day 22 一致策略
4. **加入 SUMMARY 参考节末尾**：放参考节是「查阅类」定位 · 不抢顶部 quickstart 的视觉权重

## 对话摘要

> 必备节（FAIL 级 M-FEATURE-PLAN）

按时序：
1. **task #81 (`bc816bc`) 完成后**，agent 总结时连续用了「真活」「真上线」「真活」多次
2. **用户问 2 个问题**：
   - 「你说的 MVP 是什么？」
   - 「为什么用真活这个词，这个说法好像比较低频」
3. **Agent 老实回复**：
   - MVP = Minimum Viable Product · 在本项目指 6 周完成的最小可用版本 · 列当前 ~92% 实测状态
   - 「真活」承认是自造词 · 给 3 个备选方案（A 行为改 / B 历史 sed / C 术语表）
4. **用户决策**：「做 A + C」
5. **Agent 自主执行 4 项**：
   - 写 GLOSSARY 5 类分组 + 防漂移声明
   - mdBook include + SUMMARY 链入
   - 顺手修 ROADMAP `<name>` HTML parser 告警（mdBook build 时浮出）
   - 不擅自全 sed 历史

## 验证结果

- ✅ `mdbook build`：clean · 0 error · 0 warning
- ✅ `docs/GLOSSARY.md` 体量适中（5 类 · ~70 行 · 不啰嗦）
- ✅ mdbook 导航「参考」节末尾出现「术语对照表」链接
- ⏳ Agent 行为约束（不再用「真活」）· 后续自检

## 残留 / 下一轮

- [ ] 如果浮出新自造词 → 加 GLOSSARY 一行 → 再用（防漂移机制激活）
- [ ] 后续 commit 自检：grep `git log --grep 真活` 应不再有新 commit 命中
- [ ] 用户操作 task #84 / #85（让 MVP 公开上线）
- [ ] Day 17 DevEco Plugin MVP / Day 20c onnx 链路（待架构决策）
- [x] docs/GLOSSARY.md 5 类术语对照表
- [x] mdbook include + SUMMARY 链入
- [x] ROADMAP `<name>` 反引号转义（mdBook build clean）
- [x] 防漂移声明（自此 commit 起新文档不用「真活」）
