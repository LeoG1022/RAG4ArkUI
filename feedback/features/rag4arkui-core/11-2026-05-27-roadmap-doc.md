# 11 — roadmap-doc

> 日期：2026-05-27
> 涉及代码：`docs/ROADMAP.md`（新增）、`docs/STATUS-roadmap-doc.md`（新增）、`feedback/features/rag4arkui-core/README.md`（加 Round 11 行）
> 类型：新建（纯文档 · 项目级长期归档）

## 本轮目标

把对话里给出的"全景图"持久化到 `docs/ROADMAP.md`，作为项目级长期维护文档。
后续每个 Day 完成后 agent 同步更新（不单独 commit）。

## Plan

> 必备节（FAIL 级 M-FEATURE-PLAN）

### 范围切分

| 做 | 不做 |
|---|---|
| `docs/ROADMAP.md` 完整全景图（约 200 行） | 不动 AGENTS.md / CLAUDE.md（不立强制规则，先建立惯例） |
| 配套 Round 11 feature log + STATUS-roadmap-doc | 不修改其他 docs（保持职责分离） |
| 顶部声明"维护约定"让未来 agent 知道何时更新 | 不立 ROADMAP 维护强制校验规则（先观察） |

### 文档结构（9 节）

1. 文档定位 + 维护约定（顶部）
2. 当前位置（一句话 + 4-5 行关键数据）
3. 时间线 mermaid gantt
4. 已完成（12 commit 表 · 含 STATUS 链接）
5. 剩余切片（按 Week + 优先级分组）
6. 6 周 MVP 达成度
7. 业界基线对照（§8.5 五条共识）
8. 关键里程碑预测 + 关键路径
9. 关键文档导航 + 维护约定

### 关键决策

| 决策 | 选择 | 理由 |
|---|---|---|
| 命名 | `docs/ROADMAP.md`（非 `STATUS-roadmap.md`） | ROADMAP 是跨阶段长期文档，STATUS 是单轮快照，职责不同 |
| 维护方式 | 后续 Day commit 中**piggyback** 更新（不单独 commit） | 避免每 Day 多一次 commit；ROADMAP 是低频结构调整 + 高频进度行更新 |
| 与 STATUS 的关系 | ROADMAP 引用 STATUS 链接，STATUS 不复制 ROADMAP 内容 | 单一事实源原则；ROADMAP 是 index，STATUS 是 detail |
| 是否立强制规则 | 否（先建立惯例，3-5 round 后再评估） | 避免规则膨胀（已有规则 #17 · 评估其执行成本后再扩展） |

### 与规则 #17 的兼容

本 commit 触发规则 #17：
- 新增 feature log Round 11 → 必须有 `docs/STATUS-roadmap-doc.md` 配套 → ✅
- ROADMAP 本身不算 STATUS（是另一种长期文档），不替代

## 改动要点

> API 选型 / 算法 / 关键决策 / 与上轮的差异

**无代码改动**。纯文档：
- `docs/ROADMAP.md` 新增（约 200 行 + 1 个 mermaid gantt + 5 个表）
- `docs/STATUS-roadmap-doc.md` 新增（按规则 #17 标准 6 节）
- `feedback/features/rag4arkui-core/README.md` 加 Round 11 链接

**与上轮（Day 6 评估）的差异**：
- 代码 0 行
- 是"工程治理"动作而非"功能切片"

## 对话摘要

> 必备节（FAIL 级 M-FEATURE-PLAN）

按时序：

1. **Day 6 commit 后**，agent 在结尾给"全景图位置更新"简表 + 业界基线对照
2. **用户指令**：「另外，全景图单独保存到 docs 中」
3. **Agent 决策**：
   - 文件名 `docs/ROADMAP.md`（非 `docs/STATUS-roadmap.md`，区分跨阶段 vs 单轮）
   - 内容比对话里展示的更全：加文档导航 + 维护约定
   - 顶部加"维护约定"段：让后续 agent 知道何时更新（每 Day commit 后 piggyback）
   - 触发规则 #17 → 同 commit 配 feature log + STATUS
4. **Agent 不再回问**，直接执行至本 commit

## 验证结果

- 编译：N/A（纯文档）
- 链接完整性：M-LINK-DEAD 应 PASS（ROADMAP 引用的所有相对路径均存在）
- mermaid 渲染：GitHub markdown 原生支持
- 规则 #17 校验：feature log Round 11 + docs/STATUS-roadmap-doc.md 同 commit 入库 ✓

## 残留 / 下一轮

- [ ] **维护约定执行**：从 Day 7 起，每个 Day commit 后 agent 同步更新 ROADMAP 的进度行
- [ ] 观察 3-5 round 后评估：是否需要把"每 Day 同步 ROADMAP"立为强制规则
- [ ] 未来：考虑加 `scripts/check-roadmap-sync.sh` 校验 ROADMAP 的"当前位置"与最新 commit 一致
- [x] 全景图归档到 docs/ROADMAP.md
- [x] 规则 #17 配套（Round 11 + STATUS-roadmap-doc）
